node_modules/
dist/
*.log

# Local secrets — NEVER commit
.env
.env.local

# Persisted sweep snapshots (data output, not source)
data/competitor-sweeps/*.json
!data/competitor-sweeps/.gitkeep

# Persisted margin scans (data output, not source)
data/margin-scans/*.json
!data/margin-scans/.gitkeep

# Persisted forward-intelligence outputs (data output, not source)
data/forward-intel/*.json
!data/forward-intel/.gitkeep
